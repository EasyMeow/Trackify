export default function SignInPage() {
  return (
    <section
      style={{
        display: 'flex',
        flexDirection: 'column',
        gap: 'var(--space-2)',
        padding: 'var(--space-6)',
      }}
    >
      <h1>Sign in</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Sign-in form lands in TASK-031.
      </p>
    </section>
  );
}
