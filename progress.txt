## 2026-04-26 — TASK-001: root .gitignore

What changed:
- Verified the existing root `.gitignore` already covers OS files, IDEs, .env/.env.* (with !.env.example), logs, Node (`node_modules/`, `dist/`, `build/`, `coverage/`, `.vite/`, `.tsbuildinfo`), Java/Maven (`target/`, `*.class`, `*.jar`, `.mvn/wrapper/maven-wrapper.jar`), Gradle (`.gradle/`), and Spring (`out/`, `HELP.md`).
- Marked TASK-001 status = done in tasks.json.

What was tested:
- Created temp `frontend/node_modules`, `backend/target`, `.env` paths and ran `git check-ignore -v frontend/node_modules backend/target .env`. All three reported matching rules at lines 38, 51, 25 of `.gitignore`. Cleaned up temp paths afterwards.

Follow-up risk / notes:
- Repo had no commits yet; this commit (TASK-001) becomes the initial commit and includes pre-existing untracked files (architecture.md, tasks.json, ralph.sh, .mcp.json, .claude/). Those are scaffolding for the agent loop and are intentionally not ignored.
- Next pending task: TASK-002 (root `.editorconfig`).

## 2026-04-26 — TASK-002: root .editorconfig

What changed:
- Created root `.editorconfig` with `root = true`, UTF-8, LF, final newline, trim trailing whitespace defaults, plus per-language sections: TS/JS/JSON/CSS/HTML at 2 spaces, Java at 4 spaces, YAML at 2 spaces, Markdown at 2 spaces with `trim_trailing_whitespace = false` (markdown trailing-space line breaks), and a `Makefile` tab section.
- Marked TASK-002 status = done in tasks.json.

What was tested:
- Per task spec: opened `.editorconfig` and confirmed sections are present and valid (root=true, [*], TS/JS, Java, YAML, Markdown).
- Backend not yet initialized (no `backend/gradlew`), so skipped the gradle compile/test step per user instructions.

Follow-up risk / notes:
- Indent_style/size declared in both `[*]` and language-specific blocks; harmless duplication but kept to make per-language intent explicit.
- Architecture spec uses Maven (`./mvnw`) per TASK-006/007, but the user's loop instructions reference Gradle. When backend is scaffolded, reconcile: tasks.json says Maven, user instructions say Gradle. Flagging for future task.
- Next pending task: TASK-003 (root `.env.example`).

## 2026-04-26 — TASK-003: root .env.example

What changed:
- Created root `.env.example` with placeholder values only, organized into sections matching the architecture: Frontend (`FRONTEND_ORIGIN`, `VITE_API_BASE_URL`), Backend (`SPRING_PROFILES_ACTIVE`, `SERVER_PORT`), PostgreSQL (`POSTGRES_*`, `SPRING_DATASOURCE_*`), Session (`SPRING_SESSION_STORE_TYPE=jdbc`, cookie name/HttpOnly/Secure/SameSite, `SERVER_SERVLET_SESSION_TIMEOUT`), and Auth (`AUTH_PASSWORD_ENCODER_STRENGTH`, `AUTH_PASSWORD_MIN_LENGTH`, optional `AUTH_BOOTSTRAP_ADMIN_*`).
- Marked TASK-003 status = done in tasks.json.

What was tested:
- Per task spec: opened `.env.example` and confirmed every architecture-driven variable is present with placeholder values only.
- Ran `git check-ignore -v .env .env.local .env.example` and confirmed `.env`/`.env.*` are ignored while `.env.example` is explicitly NOT ignored (matches `!.env.example` rule at line 27 of `.gitignore`).

Follow-up risk / notes:
- `SPRING_DATASOURCE_*` duplicates `POSTGRES_*` values; left both because Spring Boot reads `SPRING_DATASOURCE_*` directly while `POSTGRES_*` are useful for `docker-compose.yml` (TASK-004) and shell-level Postgres tooling. Consider trimming once compose is in place if duplication causes drift.
- `AUTH_BOOTSTRAP_ADMIN_*` placeholders are speculative — they back the `auth.bootstrap` properties class hinted at in TASK-025/026. If that path doesn't materialize, drop those keys.
- Cookie defaults are tuned for local dev (`SECURE=false`, `SAME_SITE=lax`); README/runbook (TASK-081) should call out flipping these for production HTTPS.
- Maven-vs-Gradle discrepancy from TASK-002 note still stands; not relevant to this task.
- Next pending task: TASK-004 (`docker-compose.yml` for PostgreSQL).

## 2026-04-26 — TASK-004: docker-compose.yml for PostgreSQL

What changed:
- Added root `docker-compose.yml` with a single `db` service running `postgres:16-alpine`, container name `trackify-db`, named volume `trackify-db-data`, and a `pg_isready` health check (5s interval, 10 retries, 10s start_period).
- All env values default-pulled from `.env.example`: `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_PORT`. Defaults baked in via `${VAR:-default}` so the file works even without a local `.env`.
- Marked TASK-004 status = done in tasks.json.

What was tested:
- `docker compose config --quiet` — passed (valid syntax).
- `docker compose up -d db` — pulled `postgres:16-alpine`, created network/volume/container, started successfully.
- `docker compose ps` — container `trackify-db` reported `Up 8 seconds (healthy)`, port `5432:5432` exposed.
- `docker compose down` — cleanly stopped and removed the container/network. Volume `trackify-db-data` is intentionally retained (named volume) so dev data survives `down`/`up` cycles.

Follow-up risk / notes:
- Service name is `db` (matches `docker compose up -d db` from the task spec). Backend `SPRING_DATASOURCE_URL` in `.env.example` still hardcodes `localhost:5432`, which is correct when backend runs on host. If backend ever runs inside compose, host needs to flip to `db`.
- No Spring Session schema initialization here — Flyway will own that (TASK-023). Postgres image's default initdb only creates the empty `trackify` database.
- Volume name pinned (`name: trackify-db-data`) to avoid project-prefix surprises (e.g. `trackify_trackify-db-data`). To wipe local data: `docker compose down -v`.
- Compose file uses no `version:` key (deprecated in Compose v2). If tooling on another machine still requires it, add `version: "3.9"` at the top.
- Maven-vs-Gradle note from TASK-002 still pending — relevant once TASK-006 starts.
- Next pending task: TASK-005 (root `README.md` bootstrap notes).

